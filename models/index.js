const User = require('./User'); // Importing the User model
const Thought = require('./Thought'); // Importing the Thought model

module.exports = { User, Thought }; // Exporting both User and Thought models as an object
